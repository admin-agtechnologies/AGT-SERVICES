services:
  geoloc:
    build: .
    container_name: agt-geoloc-simulator
    restart: unless-stopped
    ports:
      - "7009:7009"
    networks:
      - agt_network

networks:
  agt_network:
    external: true
    name: agt_network