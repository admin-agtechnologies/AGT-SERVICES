# ============================================================
# AGT Notification Service v1.0 - Variables d'environnement
# ============================================================

# ─── Django ─────────────────────────────────────────────────────────────────
SECRET_KEY=change-me-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,agt-notif-service

# ─── Base de données ──────────────────────────────────────────────────────────
# Hôte : agt-notif-db (défini dans docker-compose.yml)
DATABASE_URL=postgresql://notif_user:notif_password@agt-notif-db:5432/agt_notification_db

# ─── Redis ────────────────────────────────────────────────────────────────────
# Hôte : agt-notif-redis
REDIS_URL=redis://agt-notif-redis:6379/2

# ─── RabbitMQ (Broker Celery partagé) ─────────────────────────────────────────
# Hôte : agt-rabbitmq (défini dans Docker compose.infra.yml)
RABBITMQ_URL=amqp://agt_rabbit:agt_rabbit_password@agt-rabbitmq:5672//
CELERY_RESULT_BACKEND=redis://agt-notif-redis:6379/3

# ─── Auth (Validation JWT) ────────────────────────────────────────────────────
# Hôte : agt-auth-service
AUTH_SERVICE_URL=http://agt-auth-service:7000/api/v1
AUTH_SERVICE_PUBLIC_KEY_PATH=/app/keys/auth_public.pem

# ─── Users (Résolution coordonnées) ───────────────────────────────────────────
# Hôte : agt-users-service
USERS_SERVICE_URL=http://agt-users-service:7001/api/v1

# ─── Cache TTL ────────────────────────────────────────────────────────────────
USER_CACHE_TTL=300
IDEMPOTENCY_TTL=86400

# ─── Email (Développement via Mailpit) ────────────────────────────────────────
DEFAULT_FROM_EMAIL=noreply@agtechnologies.com
EMAIL_HOST=agt-mailpit
EMAIL_PORT=1025

# ─── Providers Email (Production uniquement) ──────────────────────────────────
SENDGRID_API_KEY=
MAILGUN_API_KEY=
MAILGUN_DOMAIN=

# ─── Providers SMS (Production uniquement) ────────────────────────────────────
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# ─── Push (Production uniquement) ─────────────────────────────────────────────
FCM_SERVER_KEY=

# ─── CORS ─────────────────────────────────────────────────────────────────────
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001